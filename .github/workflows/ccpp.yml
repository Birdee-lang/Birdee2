name: C/C++ CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v1
    - name: apt
      run: sudo apt-get install -y python3-dev libgc-dev
    - name: make
      run: make
    - name: make tests
      run: make tests
